//+build acme_dns_PROVIDER

package acme

import (
	"github.com/go-acme/lego/v3/challenge"
	"github.com/go-acme/lego/v3/providers/dns/PROVIDER"
)

func init() {
	providers["PROVIDER"] = func() (challenge.Provider, error) {
		return PROVIDER.NewDNSProvider()
	}
}
